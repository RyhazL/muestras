extends Layout/head.pug
block titulo
    title ZACARY: Admin Login
block content
        form.login.mainBox(action="/login", method="post")
            h1 Inicia Sesion
            input(type="text", name="login[user]" placeholder="Usuario")
            input(type="password", name="login[pass]" placeholder="Contraseña")
            input(type="submit", value="Entrar")
            if r
                h6!= r
            